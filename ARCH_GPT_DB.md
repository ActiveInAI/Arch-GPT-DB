# Arch-GPT DB

Arch-GPT DB 是基于 PanDB 的定制数据库智能工作台。PanDB 提供数据库驱动、查询、导入导出、MCP 与安全执行能力；Arch-GPT DB 在此基础上提供面向 Arch-GPT 项目的数据库上下文与 AI 工作流。

## 定制边界

- 保留 PanDB 的数据库连接、SQL 编辑器、数据网格、Schema 工具和 MCP 安全策略。
- 使用 Arch-GPT DB 的应用身份、Logo、桌面图标和 AI 助手提示。
- 首选导入已有 PanDB 数据目录，确保用户的连接与历史在首次启动时可迁移。
- 上游兼容标识（数据库文件、CLI/MCP 协议与环境变量）在迁移期保留，避免破坏现有自动化。

## 首个功能版本

1. Arch-GPT 项目上下文作为 AI SQL 助手的可选上下文来源。
2. 数据库 Schema、业务词典与项目模型的联合检索。
3. 自然语言 SQL 的生成、解释、优化与安全执行审查。
4. 面向 Arch-GPT Agent 的受限 MCP 数据库工具配置。

## 上游策略

PanDB 是上游基线。定制功能应位于可隔离的 Arch-GPT DB 层；同步上游时优先保留数据库协议、安全修复与驱动更新。
